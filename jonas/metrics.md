# The metric

They check correctness and then some other features
1 seal per second
false positives are penalized because the conveyor belt operator has to come and check. We have to pay the operator and we get penalized in Euros and also the conveyor belt gets slowed down.

Input directory & Output directory (which will save the csv file), they need requirements.txt
